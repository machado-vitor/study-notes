## Binary Tree Data Structure

This is a non-linear, hierarchical data structure, where we have:
1 - Internal Nodes, on each has at most two children, referred as left and right child.
2 - Leaf Nodes, that have no children.

The top most node is called root.

