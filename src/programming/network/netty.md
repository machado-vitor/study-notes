# Netty

Netty is an NIO client server framework.
It builds on Java NIO to provide async, event-driven network programming.

Instead of blocking threads, Netty uses:
- NioEventLoop
- NioServerSocketChannel and NioSocketChannel
- ChannelPipeline
